apiVersion: v1
kind: Service
metadata:
  name: s3cache
  labels:
    app.kubernetes.io/name: s3cache
spec:
  type: ClusterIP
  ports:
    - name: s3
      port: {{ .Values.service.port }}
      targetPort: s3
  selector:
    app.kubernetes.io/name: s3cache
