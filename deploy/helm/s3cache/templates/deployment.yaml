apiVersion: apps/v1
kind: Deployment
metadata:
  name: s3cache
  labels:
    app.kubernetes.io/name: s3cache
spec:
  replicas: {{ .Values.replicaCount }}
  # Single writer's view of the bucket → the index is authoritative. Recreate avoids
  # two proxies briefly diverging (each has its own in-memory index) during a roll.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: s3cache
  template:
    metadata:
      labels:
        app.kubernetes.io/name: s3cache
    spec:
      {{- with .Values.imagePullSecret.existingSecret }}
      imagePullSecrets:
        - name: {{ . }}
      {{- end }}
      containers:
        - name: s3cache
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: s3
              containerPort: 8014
          env:
            - name: S3CACHE_LISTEN
              value: "0.0.0.0:8014"
            - name: S3CACHE_UPSTREAM_ENDPOINT
              value: {{ required "upstream.endpoint is required" .Values.upstream.endpoint | quote }}
            - name: S3CACHE_BUCKETS
              value: {{ .Values.upstream.buckets | quote }}
            - name: RUST_LOG
              value: "info"
          # AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY / AWS_REGION for the upstream.
          envFrom:
            - secretRef:
                name: {{ .Values.auth.existingSecret }}
          # The initial full R2 sync (a paginated LIST of every configured bucket) runs
          # BEFORE the listener binds — the port only opens once the index is complete,
          # which is exactly what stops a fresh/restarted pod from serving an incomplete
          # LIST (the single-index correctness invariant on restart). A large bucket makes
          # that sync slow, so gate liveness behind a startupProbe with generous headroom;
          # without it the 10s + 3×30s liveness would kill the pod mid-sync and crashloop
          # it. Once the port opens (sync done) liveness/readiness take over normally.
          startupProbe:
            tcpSocket:
              port: s3
            periodSeconds: 5
            failureThreshold: {{ .Values.startupProbe.failureThreshold | default 120 }}
          readinessProbe:
            tcpSocket:
              port: s3
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: s3
            initialDelaySeconds: 10
            periodSeconds: 30
          resources: {{- toYaml .Values.resources | nindent 12 }}
